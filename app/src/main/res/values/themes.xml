<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ShiftApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/header_card_bg</item>
    </style>

    <style name="Theme.ShiftApp" parent="Base.Theme.ShiftApp" />
</resources>